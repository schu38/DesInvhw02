// code for a.

function setup() {
  createCanvas(340, 340);
}

function midDraw() {
  line(170, 170, random(width), random(height));
}
// code for b.

function trDraw() {
  line (340, 0, random(width), random(height));
}

// code for c. 
  
